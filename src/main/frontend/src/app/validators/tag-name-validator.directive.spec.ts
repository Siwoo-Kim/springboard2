import { TagNameValidator } from './tag-name-validator.directive';

describe('TagNameValidator', () => {
  it('should create an instance', () => {
    const directive = new TagNameValidator();
    expect(directive).toBeTruthy();
  });
});
